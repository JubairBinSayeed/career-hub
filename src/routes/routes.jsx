import { createBrowserRouter } from "react-router-dom";

const router = createBrowserRouter([
  {
    path:'/',
    element: <div className="text-center font-bold text-2xl underline mt-96">Hello World!</div>  
  }
]);
export default router